<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.ali.test">
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Route Guide"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Route Guide"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BusListActivity"
            android:label="Bus List"/>
        <activity android:name=".StopListActivity"
            android:label="Stop List"/>
        <activity android:name=".NearestStopActivity"
            android:label="Nearest Stop List"/>
        <activity android:name=".SpecificBusDetailActivity"
            android:label="Bus Detail" />
        <activity android:name=".SpecificStopDetailActivity"
            android:label="Bus Detail"
            ></activity>
    </application>

</manifest>
